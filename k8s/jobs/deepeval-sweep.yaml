# DeepEval evaluation sweep Job
# ~160 calls per quality cycle (20 questions × 8 calls/question, all four
# metrics with include_reason=False)
# NEVER run concurrently with RAGAS sweep (MLflow SQLite write contention)
apiVersion: batch/v1
kind: Job
metadata:
  name: deepeval-sweep
  labels:
    app: eval
    cycle: quality
spec:
  backoffLimit: 1
  template:
    metadata:
      labels:
        app: eval
        component: deepeval
    spec:
      restartPolicy: Never
      enableServiceLinks: false
      containers:
        - name: deepeval
          image: oss-ai-eval-jobs:latest
          imagePullPolicy: IfNotPresent
          command: ["python", "-m", "src.eval.deepeval_eval"]
          env:
            - name: VERTEX_PROJECT_ID
              valueFrom:
                secretKeyRef:
                  name: vertex-credentials
                  key: project-id
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: /secrets/service-account.json
            - name: CHATBOT_URL
              value: http://chatbot:8000
            - name: MLFLOW_TRACKING_URI
              value: http://mlflow:5000
            - name: GOLDEN_DATASET_PATH
              value: /data/golden_dataset.json
            - name: EVAL_DATASET_SIZE
              value: "20"
            - name: CLAUDE_HAIKU_MODEL
              value: claude-haiku-4-5
            - name: VERTEX_CLAUDE_LOCATION
              value: us-east5
          volumeMounts:
            - name: vertex-sa
              mountPath: /secrets
              readOnly: true
            - name: data
              mountPath: /data
              readOnly: true
      volumes:
        - name: vertex-sa
          secret:
            secretName: vertex-credentials
        - name: data
          configMap:
            name: golden-dataset
